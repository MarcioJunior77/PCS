import 'package:flutter/material.dart';

//Esse Arquivo era só um teste para a barra de navegação funcionar,
//caso vc já tenha o to-do-list pronto ou queira visualiza-lo,
//apenas troque o nome dessa classe (Test_to_do_list) para o
//arquivo que vc quer em MainPage.dart

class Test_to_do_list extends StatelessWidget {
  const Test_to_do_list({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}
